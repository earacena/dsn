contiguous.cpp is the "main.cpp" file. Currently there are bugs with the template class code. 
I will be working on changing the template class to just regular class, because i realized that implementing it is useless. 
Code currently is bugged due to template class Block. Will fix at night after classes.